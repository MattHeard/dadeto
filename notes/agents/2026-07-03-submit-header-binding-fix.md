- Unexpected hurdle: the submit-new-page authorization lookup crashed on a raw Express request because `request.get` lost its binding, and the first attempt to reuse the shared normalizer introduced JSDoc type drift.
- Diagnosis path: traced the stack from `submit-new-page-core.js` into `submit-shared.js`, confirmed the raw request path bypassed the bound getter wrapper, then let `npm run check` surface the exact type mismatches until the shared request shape lined up across `request-normalization.js` and `submit-shared.js`.
- Chosen fix: route header reads through `normalizeExpressRequest()` so the getter is bound once, broaden the request-like typedefs to match the actual caller shapes, and add a regression test that exercises a getter reading from `this.headers`.
- Next-time guidance: when a cloud helper consumes an Express request, prefer one shared request-shape normalization path early; if a gate turns red on typing after a behavior fix, treat that as a contract mismatch and align the shared typedefs instead of stacking casts.
