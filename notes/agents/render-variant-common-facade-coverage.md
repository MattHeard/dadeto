# Render variant common facade coverage

- The common-core facade was a bare star re-export and focused coverage reported 0%.
- Preserved the full export surface and added a local `ensureString` forwarding binding with direct tests.
- Evidence: bead `dadeto-ktk` records focused coverage passing 2 tests at 100% statements, branches, functions, and lines.
