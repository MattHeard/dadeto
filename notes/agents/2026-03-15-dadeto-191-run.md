# 2026-03-15: Reconcile Symphony bead lifecycle after queue reset
- **Unexpected hurdle:** The bead prompt references a “full bead-queue reset,” but no repo doc, notes, or code paths mention what that reset is or how Symphony should treat finished runs afterwards.
- **Diagnosis path:** I inspected the bead, the Symphony project note, `tracking/symphony/status.json`, and the `runs/2026-03-15T12-38-13.031Z--dadeto-191` stderr log generated when the Ralph loop started, yet none of those artifacts describe the expected transition after the reset.
- **Chosen fix:** Logged the observation here and in the bead comment so the next agent has the starting evidence and knows to supply the missing queue-reset guidance before paying down the reconciliation work.
- **Next-time guidance:** Once the reset contract is clarified (e.g., which status fields must be cleared or which `bd` commands should run), rerun the loop with that checklist to confirm `applyRunnerOutcome`/status writes land in `idle` and surface any additional automation required.
