# 2026-03-17: reconcile Symphony bead lifecycle after queue reset

- **Unexpected hurdle:** The current Symphony snapshot still shows `state: "running"` with `activeRun` present after the queue reset, but the repo does not define what the reset should do to `bd`/`status.json` beyond the existing orphaned-PID recovery path.
- **Diagnosis path:** I checked `tracking/symphony/status.json`, `src/local/symphony/app.js`, and `src/core/local/symphony.js`. The code already clears orphaned runs when a stale PID is detected, but this snapshot still has a live-looking `activeRun` record and no `lastOutcome`, so the reset contract is still missing from repo memory.
- **Chosen fix:** No code change in this loop. I recorded the current trust gap instead of guessing at lifecycle semantics.
- **Next-time guidance:** If the next pass is meant to finish reconciliation, start by defining the queue-reset contract in a repo artifact and then verify whether `applyRunnerOutcome(..., { outcome: 'blocked' })` or a completed outcome should own the post-reset state transition.
