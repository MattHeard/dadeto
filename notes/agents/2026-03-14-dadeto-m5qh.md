# 2026-03-14 dadeto-m5qh runner loop
- unexpected hurdle: The auto-loop wait helper only called the status GET endpoint, so the next pass could still see a stale `activeRun` even after Symphony had already refreshed via `/api/v1/refresh`.
- diagnosis path: Walked through `runAutoLoopCycle` and `waitForActiveRunCompletion` in `scripts/symphony-tui.js` and saw the wait loop reusing `refreshLoop()` without triggering the refresh endpoint first.
- chosen fix: Replaced the wait loop’s `refreshLoop()` call with `triggerRefresh()` so every cycle hits the `/api/v1/refresh` trigger before inspecting `activeRun` (pausing briefly between polls when the refresh fails).
- validation: `npm test`
- next-time guidance: When live Symphony access is available, confirm the refreshed `activeRun` snapshot stays current on the waiting loop before re-enabling auto mode.
