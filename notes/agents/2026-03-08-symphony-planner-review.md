# Agent Retrospective: Symphony planner review

- **Unexpected hurdle:** the Symphony project note already hinted at planner needs through open questions, but it did not yet convert those into one explicit review rhythm and ordered decision inputs.
- **Diagnosis path:** read the current project note, checked the local Symphony status/reporting surfaces, and selected the smallest set of repo artifacts that SNC can inspect without fresh code exploration.
- **Chosen fix:** added a `Planner review` section to the Symphony project note that defines when review should happen and which local docs and status artifacts should be read before creating, refreshing, or archiving a bead.
- **Next-time guidance:** if the planner review flow needs more detail, split status interpretation and bead-file lifecycle into separate doc beads instead of expanding the project note into an operations manual.
