# Agent Retrospective: Symphony project note contract

- **Unexpected hurdle:** the repo had already moved from the placeholder `example-project` scaffold toward a real `symphony` project, so the bead needed to build on those local changes instead of recreating a separate example structure.
- **Diagnosis path:** inspected the current `projects/` tree and diff, confirmed `projects/symphony/notes.md` already existed with only an Outcome section, then filled the missing planner-review sections from current local Symphony docs and tests.
- **Chosen fix:** expanded the Symphony project note with Current state, Constraints, Open questions, and four bead-sized Candidate next actions while keeping the content concise and implementation-free.
- **Next-time guidance:** once SNC starts using this note regularly, split any durable planner cadence or status-reading rules into separate Symphony doc beads instead of packing more operational policy into the project note itself.
