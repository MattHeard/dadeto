Hurdle: The hiLo toy still exposed uncovered branch logic around key release mismatch after earlier coverage slices.
Diagnosis: Coverage summary showed hiLoCardGame.js with 39/48 branches (81.25%), and the release helper was only exercised when matching keyups arrived.
Fix: Added a toy test that sends an unrelated `keyup` after a guess and asserts the state stays locked, then reran `npm test` so the coverage report now shows 40/48 branches (83.33%) for the file.
Next: If another hiLo gap appears, replay the exact keyboard sequence that hits the uncovered branch (e.g., hold one key, send a different `keydown`/`keyup`) before widening the scope.
