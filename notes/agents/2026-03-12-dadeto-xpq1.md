# Ledger ingest duplicate fixture loop (2026-03-12)
- **unexpected hurdle:** proving repeated rows stay duplicates required adding a narrowly scoped fixture before the existing duplicate test would fail in a useful way.
- **diagnosis path:** added a repeat-import fixture that replays the same dedupe key twice and extended the ledger-ingest test suite to assert the canonical/duplicate summary counts.
- **chosen fix:** landed the new fixture/test so `npm test` now records canonical=1 and duplicatesDetected=1 for the repeat import scenario, leaving a clear artifact for future runners.
- **next-time guidance:** mention the summarized canonical/duplicate counts directly in the bead evidence (or linked note) when the test proves them, so reviewers see the exact numbers without rerunning the suite.
