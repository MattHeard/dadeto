# dadeto-s5x commonCore TSDoc cleanup

- Unexpected hurdle: the shared module combines generic value helpers, filesystem adapters, and the aggregate check runner, so one compiler run exposed several unrelated inferred-`unknown` boundaries.
- Diagnosis path: grouped the 31 diagnostics into those three contracts, added explicit generic callback casts, expanded injected filesystem shapes, and typed child-process runner state and close-event inputs.
- Chosen fix: documentation/type-boundary changes only; runtime control flow and helper behavior remain unchanged.
- Evidence: focused Jest passed 3 suites / 85 tests; `npm run tsdoc:check` reports zero `src/core/commonCore.js` diagnostics; targeted lint has no errors and only unrelated existing warnings elsewhere.
- Next-time guidance: keep the check-runner dependency typedefs near `CheckCommand`/`CheckChild` so future subprocess contract edits do not fall back to `unknown`.
