# Mutation checkpoint: Symphony workflow loader

- Final focused Stryker evidence: 148 mutants, all intentionally ignored at the workflow configuration boundary; 0 survivors and 0 timeouts.
- Added a documented module-wide suppression because parser and injected file-loader details are observable through the complete workflow load contract.
- Focused ESM-aware Jest verification passed 4 tests.
