## Notes from moving src/ideas into notes

- **Challenge:** Ensuring the idea document moved without leaving stale directories or violating folder-specific instructions.
- **Resolution:** Used `git mv` to relocate `colorPalette.md` into `notes/`, then removed the empty `src/ideas` folder to keep the tree tidy.
