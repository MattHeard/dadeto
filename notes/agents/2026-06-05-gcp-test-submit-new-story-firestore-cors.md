- Unexpected hurdle: `gcp-test` still failed after the source Firestore helper had already been fixed in `src/`, because the deployed packaging tree under `infra/cloud-functions/` was stale and still used the one-argument Firestore call.
- Diagnosis path: I compared the live Cloud Run error with the generated `infra/cloud-functions/submit-new-story/firestore-helpers.js` copy, confirmed it still called `getFirestoreFn(databaseId)`, and checked the submit-new-story Express wiring for the `next is not a function` stack trace.
- Chosen fix: updated `src/core/cloud/submit-new-story/submit-new-story-core.js` to use a real 4-arg Express error middleware signature, then regenerated `infra/cloud-functions/` with `npm run build:cloud` so the deployed copy picked up the already-correct named-database helper.
- Next-time guidance: when a Cloud Run log points at a generated cloud function path, check `infra/cloud-functions/` as well as `src/`. A source fix is not enough if the deploy packaging step has not been rerun.
