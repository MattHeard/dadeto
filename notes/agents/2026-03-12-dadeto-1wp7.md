Unexpected hurdle: The hiLoCardGame branch coverage gap was buried in a large test suite and the coverage summary only pointed at general branch counts, so it wasn’t obvious which guards were untouched.
Diagnosis path: Inspected the hiLo toy source, then matched branchMap data from `reports/coverage/coverage-final.json` to map hits to lines around input parsing, state normalization, and keyboard-event handling.
Chosen fix: Added Jest slices for non-string payloads, missing event types, invalid stored scores, out-of-range cards, and ignored key events; reran `npm test` so the updated coverage summary reflects the new slices.
Next-time guidance: If we revisit this family, tie new tests to the branchMap output immediately and keep coverage notes in `notes/agents/` so future beads can pick up the exact lines.
