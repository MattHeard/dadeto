- **Unexpected hurdle:** Dadeto had already moved to SQLite, but daemon usage was still implicit because `.beads/config.yaml` left both `no-daemon` and `auto-start-daemon` commented out.
- **Diagnosis path:** Verified the active backend with `bd backend show`, checked `bd config get daemon.enabled`, inspected `.beads/config.yaml`, and re-ran `bd ready --sort priority` after the config change to confirm the workspace still answered normally.
- **Chosen fix:** Set `no-daemon: false` and `auto-start-daemon: true` in `.beads/config.yaml` so the repo explicitly allows daemon mode and will auto-start it when `bd` chooses to use it.
- **Next-time guidance / open question:** There was still no live `bd daemon` process left running after the verification command in this sandbox, so if you want evidence of actual daemon residency under load, run a dedicated daemon-health or concurrency check outside this sandbox and watch `.beads/daemon.log` plus the process table during concurrent `bd` writes.
