- Unexpected hurdle: the local GCP simulator was importing the credit V2 core directly from `src/core/cloud`, which tripped the dependency-cruiser sibling rule, and the initial handler refactor introduced lint/duplication pressure.
- Diagnosis path: I routed the simulator through a `src/core`-root shim, then used the lint and duplication reports to pinpoint the remaining warnings and clone-shaped blocks in `get-api-key-credit-v2-core.js`.
- Chosen fix: keep the transaction logic in the cloud core, but move the simulator import to `src/core/get-api-key-credit-v2.js`, add coverage for primitive snapshot payloads and the shim export, and rewrite the handler helpers to satisfy `tsdoc`, ESLint, and jscpd without changing behavior.
- Next-time guidance: if this module changes again, start by checking the `reports/lint/lint.txt` and `reports/duplication/jscpd-report.json` outputs after a focused `npm run check` pass, because the repo gates are sensitive to small helper-shape regressions.
