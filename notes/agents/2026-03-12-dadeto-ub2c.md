# dadeto-ub2c

- unexpected hurdle: jscpd kept reporting the global-event registration block in both capture handlers as duplicated, which was the next slice in the runner's pipeline.
- diagnosis path: reran `npm run duplication` to confirm the cross-handler offense and inspected the handler wiring to find the repeated `globalThis.addEventListener` + cleanup pattern.
- chosen fix: extracted `registerGlobalListener` into `captureFormShared.js` and rewired both keyboard and gamepad handlers to call that helper, keeping behavior untouched.
- next-time guidance: keep an eye on shared form wiring (top-level builder scaffolding still shows duplication) and consider whether additional small helpers can shrink that block without introducing noise.
