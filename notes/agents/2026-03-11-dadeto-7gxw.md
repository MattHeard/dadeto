# Loop 2026-03-11T20:50:58.707Z--dadeto-7gxw
- unexpected hurdle: `bd comments add` triggered the repo hook that reran `npm test`, so the loop produced two full runs back-to-back.
- diagnosis path: noticed continuous Jest output after the comment command and confirmed the command was still running rather than failing, so I waited for the duplicate run to finish.
- chosen fix: allowed both runs to complete and recorded the successful `npm test` command in bead comments, ensuring the loop evidence covers the final run.
- next-time guidance: expect `bd` hooks to rerun the full test suite when adding evidence; plan for the extra wait time when scheduling loop completion.
