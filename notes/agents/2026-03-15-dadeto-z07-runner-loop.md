- **Unexpected hurdle:** The second Beads migration path was documented, but there was no explicit verification that `.beads/metadata.json` and the bd config commands still matched reality, so I had to inspect the current repo state instead of just mirroring the list.
- **Diagnosis path:** Checked `.beads/metadata.json` for its backend/db/jsonl settings, ran `bd backend show`, `bd config get issue-prefix`, and `bd config get types.custom` to confirm the CLI still exposes the prefix/custom-type knobs that the migration steps depend on.
- **Chosen fix:** Added a “Verification snapshot” section to `projects/sqlite-beads-backend/notes.md` capturing the precise files/commands that prove the steps still work, and created bead `dadeto-5xkj` to invest in automating the gm/mh/dadeto shard import while keeping the .beads Dolt tree until the cutover is solid.
- **Next-time guidance/open question:** The next loop should introduce the script/workflow for splitting `~/.beads/issues.jsonl` by prefix and exercising the import steps so the actual second migration can be run safely.
