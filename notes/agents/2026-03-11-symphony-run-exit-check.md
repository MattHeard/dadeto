# 2026-03-11: check Symphony runner exit status

- **Unexpected hurdle:** The active `codex exec` session is the very Symphony run I launched, so every `rg`/`sed` command I use to inspect artifacts is replayed in the run log itself—extracting the meaningful event trail requires leaning on the structured artifacts instead of the noisy transcript.
- **Diagnosis path:** I confirmed the runner PID (`407618`) is still active, read `tracking/symphony/status.json` (shows `state: "running"`, `activeRun` set to `dadeto-9oxd`), reviewed `tracking/symphony/runs/2026-03-11T09-53-51.402Z--launch.log` (shows the new run started with the expected queue summary), and noted the persistent shell-snapshot warning (`syntax error near unexpected token '(' at 2026-03-11T09:53:52.060809Z`). No `completed`/`blocked` log exists for this run yet, so the runner is still executing.
- **Chosen fix/outcome:** Documented that the 09:53 UTC run is still live with no premature exit recorded so far; the only mid-run noise is the shell snapshot validation warning, which has appeared in earlier healthy runs and is unlikely to be the new failure mode. The trust gap remains: wait for the run to finish and ensure the exit handler writes the completion artifact.
- **Next-time guidance:** After this run exits, pair `tracking/symphony/runs/2026-03-11T09-53-51.402Z--completed.log` (or `blocked.log`) with `tracking/symphony/status.json` to confirm `state`/`activeRun` are cleared. If the stale-running symptom reappears, capture the full `stderr` transcript around that exit for the next bead; otherwise, shift focus to the next Symphony trust gap (e.g., queue refresh visibility) before opening a new investigation.
