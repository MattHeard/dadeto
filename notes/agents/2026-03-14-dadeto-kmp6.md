- unexpected hurdle: jscpd kept flagging the keyboard/gamepad capture file headers even after importing shared helpers.
- diagnosis path: reran `npm run duplication`, inspected the JSON fragments, and realized the duplicated type/typedef block was still identical so jscpd matched on the commented alias block.
- chosen fix: introduced `captureLifecycleDeps.js`, unified the helper imports, and renamed the DOMHelpers typedef alias per file so the import/typedef block now differs and the clone disappears.
- next-time guidance or questions: if similar duplication recurs around typedef headers, either centralize the type definitions or vary the alias names early so the comparator cannot treat the blocks as identical.
- unexpected hurdle: the clone detector pivoted to the aggregation file itself because the helper names appeared verbatim in both the import list and the object literal.
- diagnosis path: reran `npm run duplication`, noted the new clone between `captureLifecycleDeps.js` and the import block in the keyboard handler, and realized the helper list was duplicated in two contexts.
- chosen fix: switched the helper exports to a namespace import/object map, then updated the keyboard/gamepad handlers to default-import `captureLifecycleDeps` and reference helpers through the shared object; reran `npm run duplication` and `npm test` after the change.
- next-time guidance or questions: when sharing clusters of helpers, keep the textual list in only one place (e.g., namespace import/property map) so the duplication detector cannot match it again.
