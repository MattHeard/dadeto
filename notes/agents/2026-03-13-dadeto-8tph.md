# Ledger ingest invalid row (revalidation)

- **Unexpected hurdle:** None; the invalid-row fixture and structured error reporting already existed from prior loops, so this run was purely a verification pass.
- **Diagnosis path:** Confirmed `fixtures.invalidRow` lives in `src/core/browser/toys/2026-03-13/ledger-ingest/core.js`, saw the matching Jest test in `test/toys/2026-03-10/ledger-ingest.test.js`, and noted the toy spec already lists structured errors as in scope.
- **Chosen fix:** Ran the ledger-ingest fixture test to capture the current counts (errors=1, canonical=1, duplicates=0) and recorded the loop so future runners know it has already landed.
- **Next time:** If invalid-row behavior needs further expansion, add another fixture and explicit schema notes instead of re-running this verification-only loop.
