import { createGraphPlotPresenterHandle } from '../../core/browser/presenters/graphPlot.js';

const handle = createGraphPlotPresenterHandle();

export const { createGraphPlotElement } = handle;

export { handle };
