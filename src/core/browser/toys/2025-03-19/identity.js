/**
 * Simple identity function that returns the input value unchanged.
 * @template T
 * @param {T} value - Any value to be returned
 * @returns {T} The same value that was passed in
 */
export function identity(value) {
  return value;
}
