# Acceptance

- The existing delivery start point is reused exactly.
- Procurement timing is calculated backward with base duration and buffer separated.
- The result contains canonical warehouse spatial context and stock-in point identity.
