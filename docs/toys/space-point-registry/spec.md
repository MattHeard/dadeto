# SPAC8 — Space Point Registry

SPAC8 registers atemporal WGS84 locations as `{spacePointId, latitude, longitude}`, where coordinates are canonical decimal strings. Spacetime points may reference these locations through `spacePointId`.
