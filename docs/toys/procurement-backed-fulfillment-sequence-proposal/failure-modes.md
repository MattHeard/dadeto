# Failure modes

- Missing or inconsistent possession endpoints: structured invalid result.
- Invalid warehouse coordinates: structured invalid result.
- Missing, negative, or non-finite durations: structured invalid result.
- Missing, blank, duplicate, or conflicting generated IDs: structured invalid result.
- Resulting timestamps outside the existing minute-resolution contract: structured invalid result.
