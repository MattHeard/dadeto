# Acceptance

- The six operations are returned in deterministic order.
- Possession data is preserved unchanged.
- Every touching segment boundary shares the exact point ID.
- Base durations, buffers, and allocated durations remain distinguishable.
- Invalid input returns a structured failure and causes no mutation or persistence.
