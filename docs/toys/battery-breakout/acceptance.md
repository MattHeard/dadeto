# Acceptance: Battery Breakout

- The blog contains a public post for `Battery Breakout`.
- The post uses `BATT4` and defaults to `keyboard-capture` input and `canvas-2d` output.
- The toy persists state under `BATT4`.
- The toy stabilizes battery cells by charge state rather than simple clearing.
- The toy supports launch, pause, reset, paddle movement, and bounded persistence.
- The toy can reach win or lose states deterministically.
