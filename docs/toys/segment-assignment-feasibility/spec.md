# Segment Assignment Feasibility

`SEGM1` is a pure, domain-neutral predicate for inserting one candidate into an entity's bounded world line. It validates ordered intervals, overlap, touching point identity, stationary gaps, and entry/exit anchors.
