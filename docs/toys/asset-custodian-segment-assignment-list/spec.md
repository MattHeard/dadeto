# Toy Spec: Asset Custodian Segment Assignment List

`ASSE4` appends `{assetId, segmentId, custodianPersonId}` records to temporary, permanent, or envelope memory. The custodian is the person responsible for the asset during that segment. The toy is append-only and does not evaluate feasibility.
