# Acceptance

- Valid records append in order and contain only the three reference IDs.
- Missing or blank IDs return `appended: false`.
- Run `npx jest test/toys/2026-08-20/assetCustodianSegmentAssignmentList.test.js --runInBand`.
