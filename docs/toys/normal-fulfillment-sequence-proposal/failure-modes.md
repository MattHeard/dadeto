# Failure modes

- Legacy inline-coordinate possession points are rejected.
- Missing or inconsistent possession references are rejected.
- Invalid warehouse coordinates are rejected.
- Missing, negative, or non-finite durations are rejected.
- Missing, blank, duplicate, or conflicting generated IDs are rejected.
- Non-minute resulting timestamps are rejected.
