# Failure Modes

- Invalid JSON returns `false`.
- Missing arrays or proposed assignment returns `false`.
- Unknown points or segments return `false`.
- Reversed or invalid timestamps return `false`.
