# Failure Modes

- Invalid JSON or a missing assignment returns `appended: false`.
- Missing or blank IDs return `appended: false`.
- Unsupported memory locations are rejected.
- Non-list paths are rejected.
