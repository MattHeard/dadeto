# Duplication Zero

## Outcome

Reduce Dadeto duplication warnings to zero and keep them at zero while steadily increasing the duplication-detection difficulty until the threshold becomes unreasonably high.

## Current state

The repo runs `npm run duplication` via `jscpd` over `src/core`. Current config still enforces a zero duplication threshold and records reports under `reports/duplication/`. The active detection difficulty remains `minTokens: 18` in `.jscpd.json`.

After the recent helper cleanups, the old toy/helper family around `joyConMapper`, `hiLoCardGame`, and `joyConMapping` is no longer the best next target. The current remaining report is dominated by:

- one broad `gamepadCapture.js` ↔ `keyboardCapture.js` family
- several internal `gamepadCapture.js` duplicates
- a few small generic overlaps involving `hiLoCardGame.js`, `commonCore.js`, and `symphony.js`

The next smallest stable implementation slice is now a capture-lifecycle subcluster inside the cross-input-handler family rather than another toy/helper extraction.

## Constraints

Prefer small clone-cluster or helper-extraction beads over broad abstraction churn. Keep behavior stable while reducing duplication, and treat the `minTokens` value as a ratcheting difficulty knob that should only move upward when the repo stays clean at the current setting.

## Open questions

- What is the best first slice: the smallest current clone cluster, a shared helper extraction, or a threshold-ratchet bead after cleanup?
- Which clone families are genuine maintenance risks versus acceptable local repetition that should remain explicit debt?
- What should count as “unreasonably high” for the duplication threshold in this repo: a specific `minTokens` value, a clone shape, or a practical false-positive level?

## Candidate next actions

- Remove one small `gamepadCapture.js` ↔ `keyboardCapture.js` capture-lifecycle subcluster rather than attacking the whole family at once.
- Inspect the remaining `hiLoCardGame.js` spillover overlaps and decide which are meaningful maintenance risks versus generic guard noise.
- After the repo is clean at `minTokens: 18`, create a threshold-ratchet bead to raise the duplication difficulty.
- Decide what minimal regression guard should define “keep it at zero” once the threshold has been pushed higher.

## Tentative sequence

1. Remove the smallest high-signal clone clusters first instead of chasing every duplicate at once.
2. Re-run `npm run duplication` after each bead and select the next smallest stable clone cluster.
3. Now that the toy/helper family has mostly been reduced, prefer the next small `gamepadCapture.js` ↔ `keyboardCapture.js` subcluster before broader internal `gamepadCapture.js` cleanups.
4. When the report stays clean at the current `minTokens`, raise the threshold slightly and treat the new clone surface as the next queue.
5. Continue ratcheting the threshold upward only when the repo is clean at the current setting.
6. Stop the ratchet only when the threshold becomes unreasonably high for this codebase and further increases mostly create noise instead of useful pressure.

## Current duplication inventory

Current artifact: `reports/duplication/jscpd-report.json` generated by `npm run duplication` on `2026-03-09`.

Active detection setting:
- `minTokens: 18` from `.jscpd.json`
- `28` duplicate entries in the current `jscpd` report

Current clone families:
- `src/core/browser/inputHandlers/gamepadCapture.js` ↔ `src/core/browser/inputHandlers/keyboardCapture.js`
  - Largest stable cross-file family still in the report.
  - The full family is too broad for one runner-safe refactor, but it now exposes smaller subclusters.
  - Representative small subcluster entries: `gamepadCapture.js:179-185` ↔ `keyboardCapture.js:176-182` (`6` lines), `gamepadCapture.js:663-668` ↔ `keyboardCapture.js:176-181` (`5` lines), and the larger managed-form block at `gamepadCapture.js:773-789` ↔ `keyboardCapture.js:193-209` (`16` lines).
- `src/core/browser/inputHandlers/gamepadCapture.js` internal duplicates
  - Repeated short guard / reducer / event-payload fragments inside the same file.
  - Representative entries include `12` lines at `494-505` ↔ `153-163` and several `6-8` line repeats around axis and connection event handling.
- Small spillover clones outside the main families
  - `hiLoCardGame.js` self-overlaps (`5` lines)
  - `edgeWeight.js` ↔ `hiLoCardGame.js` (`5` lines)
  - `src/core/local/symphony.js` ↔ `hiLoCardGame.js` (`5` lines)
  - `blogKeyHandler.js` ↔ `keyboardCapture.js` (`5` lines)
  - `src/core/commonCore.js` ↔ `src/core/browser/jsonValueHelpers.js` (`9` lines)

Likely next implementation slice:
- Start with the smallest stable `gamepadCapture.js` ↔ `keyboardCapture.js` capture-lifecycle subcluster, not the whole family.
- Reason: the toy/helper extractions have already removed the earlier easy wins, and the remaining cross-file family now has a few `5-6` line overlaps that are small enough to shape into one bounded bead.
- Defer the broader internal `gamepadCapture.js` duplicates and generic one-off spillover clones until after one of those small cross-handler slices lands.
